<!--Using model-->
@model IEnumerable<u05038015_HW03.Models.FileModel>

@{
    ViewBag.Title = "Index";
}

<!--Heading-->
<h2>Videos</h2>
<!--Sub-heading-->
<h4>"~Media/Videos/" folder</h4>

<!--Container with light gray background-->
<!--3 Images in a row-->
<div class="container" style="background-color:lightgray;">
    <div class="row">
        <div class="column">
            @foreach (var item in Model)
            {
                <div class="col-md-4" gallery>
                    <a href="~/Media/Videos/@item.FileName" data-fancybox="gallery" data-type="video" data-caption="@item.FileName">
                        <video controls width="300" height="300">
                            <source src="~/Media/Videos/@item.FileName" type="video/mp4">
                        </video>
                    </a>
                    <p> @item.FileName</p>
                    <!--Bootstrap color button - Download-->
                    <button type="button" class="btn btn-success">
                        @Html.ActionLink("Download", "Download", new { name = item.FileName }, new { @Style = "color: black;" })
                    </button>
                    <!--Bootstrap color button - Delete-->
                    <button type="button" class="btn btn-danger">
                        @Html.ActionLink("Delete", "Delete", new { name = item.FileName }, new { @Style = "color: black;" })
                    </button>
                </div>
            }
        </div>
    </div>
</div>

<!--FancyBox-->
<script>
    $(document).ready(function () {
        $(".gallery a").fancybox();
    });
</script>

